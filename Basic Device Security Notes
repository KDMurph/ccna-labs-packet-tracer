Basic Device Security - Day 4 

> user EXEC mode
# privileged EXEC mode
(config)# global configuration mode

enable
conf t
enable password
exit

running-config - current, active configuration
startup-config - file that will be loaded on restart

write
write memory
copy run config startup config

service password-encryption - can be cracked easily
enable secret - more secure, always use

do sh run 

run is privileged-exec-level-command, executes a privileged-exec level command from global config mode

typing no deletes

show running-config - #
